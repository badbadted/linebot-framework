{
  "name": "linebot-framework",
  "version": "0.1.0",
  "description": "Reusable LINE BOT framework — regex router + plugin architecture + scheduled push",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "node-cron": "^3.0.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
